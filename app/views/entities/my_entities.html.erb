<%=  link_to 'Most recent', url_for( :recent => :true, :group => @group )%>
<%=  link_to 'Most ancient', url_for( :recent => :false, :group => @group )%>

<span> $<%= @total_amount%><span>
<ul>
    <%= render @my_entities%>
</ul>

<%= link_to "Create New Transaction", new_entity_path %>